{
  "name": "pi-browser",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Pi Browser: Firefox add-on + Pi coding agent integration over Native Messaging and ACP",
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "packages/pi-agent/extensions"
    ]
  },
  "workspaces": [
    "packages/*",
    "firefox",
    "thunderbird",
    "tests"
  ],
  "scripts": {
    "postinstall": "npm run build -w @pi-browser/protocol && npm run build -w @pi-browser/agent",
    "build": "npm run build -w @pi-browser/protocol && npm run build -w @pi-browser/webext && npm run build -w @pi-browser/agent && npm run build -w @pi-browser/firefox && npm run build -w @pi-browser/thunderbird",
    "test": "npm run test -w @pi-browser/protocol && npm run test -w @pi-browser/webext && npm run test -w @pi-browser/agent && npm run test -w @pi-browser/firefox && npm run test -w @pi-browser/thunderbird && npm run test -w pi-browser-tests",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "dependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=22"
  }
}
