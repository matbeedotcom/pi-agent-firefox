{
  "name": "@pi-browser/firefox",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "Pi Browser Firefox add-on (build workspace)",
  "exports": {
    "./mcp-server": "./dist-tests/src/background/mcp-server.js"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "tsc -p tsconfig.tests.json && node --test dist-tests/test/*.test.js"
  },
  "dependencies": {
    "@pi-browser/protocol": "0.1.1",
    "@pi-browser/webext": "^0.1.1"
  },
  "devDependencies": {
    "@types/firefox-webext-browser": "^143.0.0",
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
